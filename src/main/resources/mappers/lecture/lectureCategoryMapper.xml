<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.care.bedu.lecture.dao.LectureCategoryDAO">

    <select id="getCategories" resultType="com.care.bedu.lecture.vo.LectureCategoriesVO">
        select
            NUM,
            CATE_CODE as cateCode,
            CATE_KOR as cateKor,
            ICON,
            null as parentCode,
            null as parentNum
        from
            t_bedu_lect_top_cate
        union all
        select 
            num,
            CATE_CODE as cateCode,
            CATE_KOR as cateKor,
            PARENT_NUM as parentNum,
            PARENT_CODE as parentCode,
            null as icon
        from 
            t_bedu_lect_mid_cate
        union all
        select
            num,
            CATE_CODE as cateCode,
            CATE_KOR as cateKor,
            PARENT_NUM parnetNum,
            PARENT_CODE parentCode,
            null as icon
        from 
            t_bedu_lect_bot_cate
        order by 
            NUM;
    </select>

    <select id="getBot" resultType="com.care.bedu.lecture.vo.LectureCategoriesVO">
        SELECT 
            num,
            CATE_CODE as cateCode,
            CATE_KOR as cateKor,
            PARENT_CODE as parentCode,
            parent_num as parentNum 
        FROM 
            t_bedu_lect_bot_cate 
        WHERE PARENT_CODE = #{mid}
    </select>


</mapper>