<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.care.bedu.community.reply.dao.ReplyDAO">
	
	<insert id="viewWrite" parameterType="com.care.bedu.community.reply.vo.ReplyVO">
		INSERT INTO t_bedu_reply(USER_ID, content, COMM_NUM, DATE, FAQ_NUM, RW_NUM, lecturenum, REG_DATE, REG_ID)
		VALUES(#{userId}, #{content}, #{commNum}, SYSDATE(), #{faqNum}, #{rwNum}, #{lecturenum}, SYSDATE(), #{regId})
	</insert>
	
</mapper>