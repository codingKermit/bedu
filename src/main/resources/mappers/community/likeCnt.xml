<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.care.bedu.community.qna.dao.LikeCntDAO">
	
	<insert id="likeCntSave">
		INSERT INTO T_BEDU_LIKE_CNT_QS(
	      QS_LIKE_BD_NUM,
		  USER_NAME,
		  QS_LIKE_YN,
		  QS_BD_NUM,
		  QS_LIKE_DATE, 
		  REG_ID
		)VALUES(
		  BEDU_LIKE_QS.NEXTVAL,
		  #{userName},
		  1,
		  #{qsBdNum},
		  SYSDATE,
		  #{regId}
		)
	</insert>
	
	<select id="getlikenum" resultType="int">
		SELECT
		 QS_LIKE_BD_NUM 
		FROM T_BEDU_LIKE_CNT_QS
		WHERE QS_BD_NUM = #{qnum} and USER_NAME = #{userName}
	</select>

	
	<delete id="likedel">
		delete
		 from T_BEDU_LIKE_CNT_QS
		WHERE QS_LIKE_BD_NUM = #{num}
	</delete>
	
</mapper>